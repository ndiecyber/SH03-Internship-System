import { createPageMetadata } from "@/utils/create-page-metadata";
import { DashboardPlaceholder } from "@/components/shared/dashboard-placeholder";

export const metadata = createPageMetadata("Internship Registration");

export default function InternshipRegistrationPage() {
  return <DashboardPlaceholder title="Internship Registration" />;
}
